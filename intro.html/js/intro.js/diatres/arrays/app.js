/* const person1 = {
  nombre: "Joe",
  apellido: "doe",
};
const person2 = {
  nombre: "Jane",
  apellido: "doe",
};
const person3 = {
  nombre: "John",
  apellido: "doe",
};
/ responses de data /
const response = {
  status: 200,
  message: "ok",
  data: persons,
};

const persons = [person1, person2, person3];

console.log(response); */

/* numbers.push(4); //agrega un numero al final
numbers.pop(); //quita un numero al final
numbers.unshift(-1); //agrega un numero al principio
numbers.shift(); //quita el primer numero
console.log(numbers);
console.log(numbers.flat(-1)); //devuelve el ultimo numero de un array
console.log(numbers.slice(0, 3)); //va por indices y arranca en 0, no elimina el array pero si toma una porcion
console.log(numbers.splice(2, 2)); // arranca en la posicion que quiero y el segundo parametro es la cantidad de elementos que quiero. elimina el array
numbers[0] = 345; //sobre escritura

console.log(numbers); */

let numbers = [1, 2, 3, 4, 5, 7, 8, 9];

//for normal

/* for (let i = 0; i < numbers.length; i++) {
  const element = numbers[i];
  console.log(element);
} */

//forof

/* for (const element of numbers) { 
  console.log(element);
} */

//forin
/* for (let i in numbers) {
  console.log(numbers[i]);
} */


